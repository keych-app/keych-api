/**
 * Keych API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CodeInfo from './CodeInfo';
import ProductInfo from './ProductInfo';

/**
 * The ExecuteMessagePostRequest model module.
 * @module model/ExecuteMessagePostRequest
 * @version 1.0.0
 */
class ExecuteMessagePostRequest {
    /**
     * Constructs a new <code>ExecuteMessagePostRequest</code>.
     * @alias module:model/ExecuteMessagePostRequest
     * @param collectionCodeInfo {module:model/CodeInfo} 
     * @param productTemplate {module:model/ProductInfo} 
     */
    constructor(collectionCodeInfo, productTemplate) { 
        
        ExecuteMessagePostRequest.initialize(this, collectionCodeInfo, productTemplate);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, collectionCodeInfo, productTemplate) { 
        obj['collection_code_info'] = collectionCodeInfo;
        obj['product_template'] = productTemplate;
    }

    /**
     * Constructs a <code>ExecuteMessagePostRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ExecuteMessagePostRequest} obj Optional instance to populate.
     * @return {module:model/ExecuteMessagePostRequest} The populated <code>ExecuteMessagePostRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ExecuteMessagePostRequest();

            if (data.hasOwnProperty('owner')) {
                obj['owner'] = ApiClient.convertToType(data['owner'], 'String');
            }
            if (data.hasOwnProperty('entropy')) {
                obj['entropy'] = ApiClient.convertToType(data['entropy'], 'String');
            }
            if (data.hasOwnProperty('collection_code_info')) {
                obj['collection_code_info'] = ApiClient.convertToType(data['collection_code_info'], CodeInfo);
            }
            if (data.hasOwnProperty('product_template')) {
                obj['product_template'] = ApiClient.convertToType(data['product_template'], ProductInfo);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ExecuteMessagePostRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ExecuteMessagePostRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ExecuteMessagePostRequest.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['owner'] && !(typeof data['owner'] === 'string' || data['owner'] instanceof String)) {
            throw new Error("Expected the field `owner` to be a primitive type in the JSON string but got " + data['owner']);
        }
        // ensure the json data is a string
        if (data['entropy'] && !(typeof data['entropy'] === 'string' || data['entropy'] instanceof String)) {
            throw new Error("Expected the field `entropy` to be a primitive type in the JSON string but got " + data['entropy']);
        }
        // validate the optional field `collection_code_info`
        if (data['collection_code_info']) { // data not null
          CodeInfo.validateJSON(data['collection_code_info']);
        }
        // validate the optional field `product_template`
        if (data['product_template']) { // data not null
          ProductInfo.validateJSON(data['product_template']);
        }

        return true;
    }


}

ExecuteMessagePostRequest.RequiredProperties = ["collection_code_info", "product_template"];

/**
 * A human readable address.  In Cosmos, this is typically bech32 encoded. But for multi-chain smart contracts no assumptions should be made other than being UTF-8 encoded and of reasonable length.  This type represents a validated address. It can be created in the following ways 1. Use `Addr::unchecked(input)` 2. Use `let checked: Addr = deps.api.addr_validate(input)?` 3. Use `let checked: Addr = deps.api.addr_humanize(canonical_addr)?` 4. Deserialize from JSON. This must only be done from JSON that was validated before such as a contract's state. `Addr` must not be used in messages sent by the user because this would result in unvalidated instances.  This type is immutable. If you really need to mutate it (Really? Are you sure?), create a mutable copy using `let mut mutable = Addr::to_string()` and operate on that `String` instance.
 * @member {String} owner
 */
ExecuteMessagePostRequest.prototype['owner'] = undefined;

/**
 * entropy provided by the user- wil be used as entropy for the generated collections
 * @member {String} entropy
 */
ExecuteMessagePostRequest.prototype['entropy'] = undefined;

/**
 * @member {module:model/CodeInfo} collection_code_info
 */
ExecuteMessagePostRequest.prototype['collection_code_info'] = undefined;

/**
 * @member {module:model/ProductInfo} product_template
 */
ExecuteMessagePostRequest.prototype['product_template'] = undefined;






export default ExecuteMessagePostRequest;

