/**
 * Keych API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Trait model module.
 * @module model/Trait
 * @version 1.0.0
 */
class Trait {
    /**
     * Constructs a new <code>Trait</code>.
     * attribute trait
     * @alias module:model/Trait
     * @param value {String} trait value
     */
    constructor(value) { 
        
        Trait.initialize(this, value);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, value) { 
        obj['value'] = value;
    }

    /**
     * Constructs a <code>Trait</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Trait} obj Optional instance to populate.
     * @return {module:model/Trait} The populated <code>Trait</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Trait();

            if (data.hasOwnProperty('display_type')) {
                obj['display_type'] = ApiClient.convertToType(data['display_type'], 'String');
            }
            if (data.hasOwnProperty('max_value')) {
                obj['max_value'] = ApiClient.convertToType(data['max_value'], 'String');
            }
            if (data.hasOwnProperty('trait_type')) {
                obj['trait_type'] = ApiClient.convertToType(data['trait_type'], 'String');
            }
            if (data.hasOwnProperty('value')) {
                obj['value'] = ApiClient.convertToType(data['value'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Trait</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Trait</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Trait.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['display_type'] && !(typeof data['display_type'] === 'string' || data['display_type'] instanceof String)) {
            throw new Error("Expected the field `display_type` to be a primitive type in the JSON string but got " + data['display_type']);
        }
        // ensure the json data is a string
        if (data['max_value'] && !(typeof data['max_value'] === 'string' || data['max_value'] instanceof String)) {
            throw new Error("Expected the field `max_value` to be a primitive type in the JSON string but got " + data['max_value']);
        }
        // ensure the json data is a string
        if (data['trait_type'] && !(typeof data['trait_type'] === 'string' || data['trait_type'] instanceof String)) {
            throw new Error("Expected the field `trait_type` to be a primitive type in the JSON string but got " + data['trait_type']);
        }
        // ensure the json data is a string
        if (data['value'] && !(typeof data['value'] === 'string' || data['value'] instanceof String)) {
            throw new Error("Expected the field `value` to be a primitive type in the JSON string but got " + data['value']);
        }

        return true;
    }


}

Trait.RequiredProperties = ["value"];

/**
 * indicates how a trait should be displayed
 * @member {String} display_type
 */
Trait.prototype['display_type'] = undefined;

/**
 * optional max value for numerical traits
 * @member {String} max_value
 */
Trait.prototype['max_value'] = undefined;

/**
 * name of the trait
 * @member {String} trait_type
 */
Trait.prototype['trait_type'] = undefined;

/**
 * trait value
 * @member {String} value
 */
Trait.prototype['value'] = undefined;






export default Trait;

